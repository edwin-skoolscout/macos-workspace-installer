# macOS only: Stripe CLI and GUI apps
# (the stripe tap formula has an unsatisfied requirement on Linux; use Stripe's apt repo there)
tap "stripe/stripe-cli"

brew "stripe/stripe-cli/stripe"

cask "ghostty"
cask "visual-studio-code"
cask "google-chrome"
cask "postman"
cask "figma"
